import fs from 'node:fs'
import path from 'node:path'

const file = path.resolve('src/App.tsx')
let source = fs.readFileSync(file, 'utf8')
if (source.includes('MINISTER_PHOTO_POSITIONING_V1')) process.exit(0)

source = source.replace("  image_url_3?: string\n}", "  image_url_3?: string\n  image_fit?: string\n  image_zoom?: number\n  image_position_x?: number\n  image_position_y?: number\n  image_2_fit?: string\n  image_2_zoom?: number\n  image_2_position_x?: number\n  image_2_position_y?: number\n  image_3_fit?: string\n  image_3_zoom?: number\n  image_3_position_x?: number\n  image_3_position_y?: number\n}")
source = source.replace("  photos: string[]\n}", "  photos: string[]\n  photoSettings: { fit:string; zoom:number; x:number; y:number }[]\n}")
source = source.replace("const [ministers, setMinisters] = useState<MinisterItem[]>(DEFAULT_MINISTERS.map((m: any) => ({ ...m, photos: [m.img].filter(Boolean) })))", "const [ministers, setMinisters] = useState<MinisterItem[]>(DEFAULT_MINISTERS.map((m: any) => ({ ...m, photos: [m.img].filter(Boolean), photoSettings: [{fit:'cover',zoom:100,x:50,y:50}] })))")
source = source.replace("            photos: uniquePhotos.length ? uniquePhotos : [IMGS.pastor],\n          }", "            photos: uniquePhotos.length ? uniquePhotos : [IMGS.pastor],\n            photoSettings: [\n              {fit:m.image_fit==='contain'?'contain':'cover',zoom:Number(m.image_zoom)||100,x:Number(m.image_position_x)||50,y:Number(m.image_position_y)||50},\n              {fit:m.image_2_fit==='contain'?'contain':'cover',zoom:Number(m.image_2_zoom)||100,x:Number(m.image_2_position_x)||50,y:Number(m.image_2_position_y)||50},\n              {fit:m.image_3_fit==='contain'?'contain':'cover',zoom:Number(m.image_3_zoom)||100,x:Number(m.image_3_position_x)||50,y:Number(m.image_3_position_y)||50},\n            ],\n          }")
source = source.replace("const photo = m.photos[currentPhoto] || m.img\n            return (", "const photo = m.photos[currentPhoto] || m.img\n            const settings = (m.photoSettings || [])[currentPhoto] || {fit:'cover',zoom:100,x:50,y:50}\n            return (")
source = source.replace("style={{ width: '100%', height: '100%', objectFit: 'cover', filter: 'brightness(0.85) saturate(0.85)', transition: 'opacity .25s ease, transform .45s ease' }}", "style={{ width: '100%', height: '100%', objectFit: settings.fit as any, objectPosition: settings.x + '% ' + settings.y + '%', transform: 'scale(' + (settings.zoom / 100) + ')', transformOrigin: 'center center', filter: 'brightness(0.85) saturate(0.85)', transition: 'opacity .25s ease, transform .45s ease, object-position .2s ease' }}")
source = source.replace("<img src={selectedMinister.photos[modalPhotoIndex] || selectedMinister.img} alt={selectedMinister.name} style={{ width: '100%', height: '100%', objectFit: 'cover' }} />", "<img src={selectedMinister.photos[modalPhotoIndex] || selectedMinister.img} alt={selectedMinister.name} style={{ width: '100%', height: '100%', objectFit: (selectedMinister.photoSettings?.[modalPhotoIndex]?.fit || 'cover') as any, objectPosition: (selectedMinister.photoSettings?.[modalPhotoIndex]?.x ?? 50) + '% ' + (selectedMinister.photoSettings?.[modalPhotoIndex]?.y ?? 50) + '%', transform: 'scale(' + ((selectedMinister.photoSettings?.[modalPhotoIndex]?.zoom || 100) / 100) + ')', transformOrigin: 'center center' }} />")

// Keep the minister bio readable on phones: the modal becomes a single-column
// sheet, the photo gets a shorter viewport, and the long biography can scroll
// without being squeezed beside the image.
source = source.replace(
  "<section id=\"ministers\" style={{ padding: '140px 24px 100px', background: 'var(--bg2)', position: 'relative', minHeight: '80vh' }}>",
  "<section id=\"ministers\" style={{ padding: '140px 24px 100px', background: 'var(--bg2)', position: 'relative', minHeight: '80vh' }}><style>{`\n@media (max-width: 700px) {\n  .hilltop-minister-modal { padding: 10px !important; align-items: flex-end !important; }\n  .hilltop-minister-dialog { max-height: 94vh !important; padding: 18px !important; border-radius: 20px 20px 0 0 !important; overflow-y: auto !important; -webkit-overflow-scrolling: touch; }\n  .hilltop-minister-layout { grid-template-columns: 1fr !important; gap: 18px !important; }\n  .hilltop-minister-photo { height: min(52vh, 330px) !important; border-radius: 14px !important; }\n  .hilltop-minister-copy { padding-top: 0 !important; min-width: 0; }\n  .hilltop-minister-copy h3 { font-size: clamp(1.7rem, 8vw, 2.25rem) !important; line-height: 1.08 !important; margin-bottom: 16px !important; padding-right: 34px !important; overflow-wrap: anywhere; }\n  .hilltop-minister-bio { font-size: .96rem !important; line-height: 1.75 !important; overflow-wrap: anywhere; word-break: normal; white-space: pre-line; }\n  .hilltop-minister-close { top: 12px !important; right: 12px !important; width: 36px !important; height: 36px !important; }\n}\n@media (max-width: 380px) {\n  .hilltop-minister-dialog { padding: 14px !important; }\n  .hilltop-minister-photo { height: 250px !important; }\n  .hilltop-minister-bio { font-size: .92rem !important; line-height: 1.7 !important; }\n}\n`}</style>")
source = source.replace("<div style={{ position: 'fixed', inset: 0, zIndex: 1000, background: 'rgba(8,8,14,0.88)', backdropFilter: 'blur(10px)', display: 'flex', alignItems: 'center', justifyContent: 'center', padding: 20 }}", "<div className=\"hilltop-minister-modal\" style={{ position: 'fixed', inset: 0, zIndex: 1000, background: 'rgba(8,8,14,0.88)', backdropFilter: 'blur(10px)', display: 'flex', alignItems: 'center', justifyContent: 'center', padding: 20 }}")
source = source.replace("<div style={{ background: '#12121c', border: '1px solid var(--border-hi)', borderRadius: 24, width: '100%', maxWidth: 820, maxHeight: '88vh', overflowY: 'auto', padding: '32px', position: 'relative', boxShadow: '0 25px 70px rgba(0,0,0,0.65)' }}", "<div className=\"hilltop-minister-dialog\" style={{ background: '#12121c', border: '1px solid var(--border-hi)', borderRadius: 24, width: '100%', maxWidth: 820, maxHeight: '88vh', overflowY: 'auto', padding: '32px', position: 'relative', boxShadow: '0 25px 70px rgba(0,0,0,0.65)' }}")
source = source.replace("<button onClick={() => setSelectedMinister(null)} aria-label=\"Close minister details\"", "<button className=\"hilltop-minister-close\" onClick={() => setSelectedMinister(null)} aria-label=\"Close minister details\"")
source = source.replace("<div style={{ display: 'grid', gridTemplateColumns: 'minmax(260px, .9fr) minmax(0, 1.1fr)', gap: 30, alignItems: 'start' }}>", "<div className=\"hilltop-minister-layout\" style={{ display: 'grid', gridTemplateColumns: 'minmax(260px, .9fr) minmax(0, 1.1fr)', gap: 30, alignItems: 'start' }}>")
source = source.replace("<div style={{ position: 'relative', height: 360, borderRadius: 18, overflow: 'hidden', background: '#0f0f18' }}>", "<div className=\"hilltop-minister-photo\" style={{ position: 'relative', height: 360, borderRadius: 18, overflow: 'hidden', background: '#0f0f18' }}>")
source = source.replace("<div style={{ paddingTop: 10 }}>", "<div className=\"hilltop-minister-copy\" style={{ paddingTop: 10 }}>")
source = source.replace("<div style={{ fontFamily: 'Outfit', fontSize: '0.92rem', color: 'var(--muted)', lineHeight: 1.8, whiteSpace: 'pre-line' }}>{selectedMinister.desc}</div>", "<div className=\"hilltop-minister-bio\" style={{ fontFamily: 'Outfit', fontSize: '0.92rem', color: 'var(--muted)', lineHeight: 1.8, whiteSpace: 'pre-line', overflowWrap: 'anywhere' }}>{selectedMinister.desc}</div>")

source = source.replace("/* MINISTER_GALLERY_V1 */", "/* MINISTER_GALLERY_V1 */\n/* MINISTER_PHOTO_POSITIONING_V1 */")
fs.writeFileSync(file, source)
console.log('Public minister photo positioning and mobile bio layout applied')
