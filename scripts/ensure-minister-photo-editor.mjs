// Minister editor source is maintained directly in AdminPortal.tsx.
// This build step is intentionally non-mutating so repeated Vercel builds
// cannot introduce duplicate React state declarations.
console.log('Minister editor source maintained directly; no build-time mutation required.')
